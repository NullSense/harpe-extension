{
  "name": "harpe-extension",
  "private": true,
  "description": "Harpe browser extension — dev tooling only (the extension itself ships from extension/).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "package": "scripts/package.sh"
  }
}
